---
import Base from '@/layouts/base.astro';

const posts = await Astro.glob('./about.md');
const { Content, frontmatter } = posts[0];
---

<Base frontmatter={frontmatter}>
  <article class="markdown-body">
    <Content/>
  </article>
</Base>
